target-version = "py310"
line-length = 100

exclude = [
  ".git",
  ".venv",
  ".venv-diffnet",
  "__pycache__",
  "build",
  "dist",
  "outputs",
  "third_party",
]

[lint]
select = ["E4", "E7", "E9", "F", "I"]
